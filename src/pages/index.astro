---
import Base from '../layouts/Base.astro'
import SiteHeader from '../components/svelte/SiteHeader.svelte'
import LiveFeed from '../components/svelte/LiveFeed.svelte'
---

<Base
    title="Live EVE Online Killboard & Killmail Feed | Socket.Kill"
    ogTitle="Socket.Kill"
    ogDescription="Real-time EVE Online killboard stream, pushed the moment they land. Watch New Eden's losses stream live, filtered by system, ship, corporation or alliance."
>
    <div class="crt-screen max-w-[1700px] mx-auto px-4 py-3">
        <SiteHeader client:load />
        <main class="mt-4">
            <h1 class="sr-only">Socket.Kill — Live EVE Online Killboard</h1>
            <LiveFeed client:only="svelte" />
        </main>
    </div>
</Base>